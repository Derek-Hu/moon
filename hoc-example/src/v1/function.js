function addAndLog(x, y) {
    var result = x + y;
    return result;
}

function multiplyAndLog(x, y) {
    var result = x * y;
    return result;
}
function log(msg) {
    console.log(msg);
}
const Pages = {
    home: {}
};
function transform(code) {
    // analysis AST
    // @Controller
    // AST CURD
    // AST --> NewCode
    return newCode;
}

import 'home';

webpack--  start > compile-- > end